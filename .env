# Frontend environment variables
# Point this at your deployed Render backend. MUST include the /api suffix.
# Locally you can comment it out -> defaults to http://localhost:8000/api
VITE_API_BASE_URL=https://bewerbung-boot-backend.onrender.com/api
